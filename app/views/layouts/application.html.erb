<!DOCTYPE html>
<html>
  <head>
    <title>PlanB App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags :defaults%>
  </head>

  <body>
    <% if user_signed_in?%>
      <%= render "shared/navbar" %>
    <% end %>
    <% if notice.present? || alert.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
      <p class="alert p-4 mb-4 text-sm text-red-700 bg-red-100 rounded-lg dark:bg-red-200 dark:text-red-800"><%= alert %></p>
    <% end %>
    
    <% unless current_page?(new_user_registration_path) || current_page?(new_user_session_path) %>
      <%= render "shared/splash_page" %>
    <% end %>
    <%= yield %>
  </body>
</html>
